const test = document.querySelectorAll(".inline div");
const screenRes = document.getElementById("result");
const additions = document.getElementById("adds");
let result = false;

for (let i = 0; i < test.length; i++) {
  test[i].addEventListener("click", () => {});
}
